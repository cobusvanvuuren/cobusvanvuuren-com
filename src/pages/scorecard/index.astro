---
// Preserve any query string (e.g. UTM params on old inbound links) instead of
// dropping it. A bare '/score' redirect silently discards it.
return Astro.redirect(`/score${Astro.url.search}`, 301);
---
